namespace MobileDemo
{
    public static class Globals
    {
        /// <summary>
        /// This handles the Navigation of pages.
        /// It ensures pages can not get pushed to the Navigation stack twice when a button is pressed twice.
        /// </summary>
        public static bool LockNavigation;
    }
}
